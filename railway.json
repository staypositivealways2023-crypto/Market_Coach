{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "python-backend/Dockerfile",
    "buildContext": "python-backend"
  },
  "deploy": {
    "healthcheckPath": "/health",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
