enum AssetClass { stock, crypto, etf, marketIndex }

enum SignalDirection { bullish, bearish, neutral }

enum LessonLevel { beginner, intermediate, advanced }

enum LessonStatus { unread, inProgress, completed }

enum SubscriptionTier { free, pro }

enum TradeDirection { long, short }

enum TradeStatus { open, closed, cancelled }

enum NewsSentiment { bullish, bearish, neutral }

enum TimeInterval { oneMinute, fiveMinutes, fifteenMinutes, oneHour, oneDay }

enum TimePeriod { oneDay, oneWeek, oneMonth, threeMonths, oneYear }
